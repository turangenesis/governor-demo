# Core agent stack
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-aws>=0.2.0          # Bedrock provider (sponsor path)
langchain-anthropic>=0.3.0    # Anthropic API provider (simplest local path)
boto3>=1.34.0

# Config + HTTP
python-dotenv>=1.0.0
requests>=2.31.0

# UI (Step 6)
streamlit>=1.36.0
